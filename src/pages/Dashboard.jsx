import { useMemo } from 'react';import preferences from '../config/jobPreferences';import { ArrowRight,BriefcaseBusiness,CalendarDays,Radio,Sparkles,Star } from 'lucide-react';import { Link } from 'react-router-dom';import { Bar,BarChart,CartesianGrid,ResponsiveContainer,Tooltip,XAxis,YAxis } from 'recharts';import JobTable from '../components/JobTable';import { useJobs } from '../context/JobContext';import { dashboardCounters,sortJobs } from '../services/jobService'
export default function Dashboard(){const{jobs}=useJobs();const counts=useMemo(()=>dashboardCounters(jobs),[jobs]);const stats=[['Total Jobs',counts.total,BriefcaseBusiness],['New Jobs',counts.new,Sparkles],['Saved Jobs',counts.saved,Star],['Applied',counts.applied,ArrowRight],['Interviews',counts.interviews,CalendarDays],['Rejected',counts.rejected,Radio],['Offers',counts.offers,Sparkles]];const insights=[['Found today',counts.today],['Found this week',counts.week],['80%+ Match Jobs',counts.good],['90%+ Match Jobs',counts.excellent],['Remote Jobs',counts.remote]];const chart=[['New',counts.new],['Saved',counts.saved],['Applied',counts.applied],['Interview',counts.interviews],['Offer',counts.offers]].map(([name,value])=>({name,value}));return <><div className="welcome"><div><p className="eyebrow">CV-DRIVEN JOB DISCOVERY</p><h2>Your next opportunity is<br/><span>ranked automatically.</span></h2><p>Real permitted feeds populate the dashboard. You review and submit manually.</p></div><div className="profile-card"><p className="eyebrow">TARGET PROFILE</p><strong>{preferences.candidateProfile.title}</strong><dl><div><dt>Experience</dt><dd>{preferences.candidateProfile.professionalExperience}</dd></div><div><dt>Preferred range</dt><dd>{preferences.candidateProfile.preferredExperienceRange}</dd></div><div><dt>Locations</dt><dd>{preferences.locations.join(', ')}</dd></div></dl><span className="scan-pill"><i/>Scheduled discovery active <small>Manual application only</small></span></div></div><div className="stat-grid">{stats.map(([label,value,Icon])=><article className="stat-card" key={label}><div><span>{label}</span><strong>{value}</strong></div><Icon/></article>)}</div><div className="insight-grid">{insights.map(([label,value])=><article key={label}><span>{label}</span><strong>{value}</strong></article>)}</div><div className="dashboard-grid"><section className="panel chart-panel"><div className="section-head"><div><p className="eyebrow">PIPELINE</p><h3>Application progress</h3></div></div><ResponsiveContainer width="100%" height={260}><BarChart data={chart}><CartesianGrid strokeDasharray="3 3" stroke="#1b2c40" vertical={false}/><XAxis dataKey="name" stroke="#718096"/><YAxis allowDecimals={false} stroke="#718096"/><Tooltip contentStyle={{background:'#0d1c2e',border:'1px solid #24364d'}}/><Bar dataKey="value" fill="#20d5b3" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer></section><section className="panel focus-panel"><p className="eyebrow">WEEKLY FOCUS</p><h3>Keep momentum</h3><div className="ring"><strong>{counts.good}</strong><span>good matches</span></div><p>Review high-match jobs before their listings close.</p><Link className="button button-primary" to="/jobs">Browse jobs <ArrowRight size={16}/></Link></section></div><section className="panel"><div className="section-head"><div><p className="eyebrow">TOP MATCHES</p><h3>Worth your attention</h3></div><Link to="/jobs">View all <ArrowRight size={15}/></Link></div><JobTable jobs={sortJobs(jobs).slice(0,4)} onEdit={()=>{}}/></section></>}
