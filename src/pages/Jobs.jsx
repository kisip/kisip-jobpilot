import { useMemo,useState } from 'react'
import { Filter,Plus,Search } from 'lucide-react'
import { useJobs } from '../context/JobContext'
import { sortJobs } from '../services/jobService'
import JobForm from '../components/JobForm';import JobTable from '../components/JobTable'
const quicks=['All','Excellent Match','80%+','Remote','0–2 Years','1–2 Years','New Today','DevOps','Linux Admin','SysAdmin','SRE']
const quickMatch=(job,quick)=>{if(quick==='All')return true;if(quick==='Excellent Match')return job.matchScore>=90;if(quick==='80%+')return job.matchScore>=80;if(quick==='Remote')return job.workMode==='Remote'||/remote/i.test(job.location);if(quick==='New Today')return job.dateDiscovered===new Date().toISOString().slice(0,10);if(quick==='0–2 Years')return /0\s*[–-]\s*2|junior|entry|1\s*(?:\+|year)|2\s*years?/i.test(`${job.experience} ${job.title}`);if(quick==='1–2 Years')return /1\s*[–-]\s*2/i.test(job.experience);if(quick==='DevOps')return /devops/i.test(job.title);if(quick==='Linux Admin')return /linux administrator|linux admin/i.test(job.title);if(quick==='SysAdmin')return /systems? administrator|sysadmin/i.test(job.title);return /site reliability|\bsre\b/i.test(job.title)}
export default function Jobs({preset}){const{jobs}=useJobs();const[search,setSearch]=useState(''),[status,setStatus]=useState(preset==='Saved'?'Saved':'All'),[mode,setMode]=useState('All'),[sort,setSort]=useState('match'),[quick,setQuick]=useState('All'),[modal,setModal]=useState(false),[edit,setEdit]=useState(null);const displayed=useMemo(()=>{const filtered=jobs.filter(j=>preset==='applications'?['Applied','Interview','Rejected','Offer'].includes(j.status):true).filter(j=>status==='All'||j.status===status).filter(j=>mode==='All'||j.workMode===mode).filter(j=>quickMatch(j,quick)).filter(j=>`${j.title} ${j.company} ${j.location} ${j.skills.join(' ')}`.toLowerCase().includes(search.toLowerCase()));return sort==='match'?sortJobs(filtered):[...filtered].sort((a,b)=>new Date(b.dateDiscovered)-new Date(a.dateDiscovered))},[jobs,preset,status,mode,search,sort,quick]);return <><div className="page-heading"><div><p>{displayed.length} automatically discovered opportunities</p></div><button className="button button-primary" onClick={()=>setModal(true)}><Plus size={18}/> Add Job Manually</button></div><div className="quick-filters">{quicks.map(item=><button key={item} className={quick===item?'active':''} onClick={()=>setQuick(item)}>{item}</button>)}</div><div className="toolbar"><label className="search"><Search/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search roles, companies, skills…"/></label><label><Filter size={16}/><select value={status} onChange={e=>setStatus(e.target.value)}><option>All</option>{['New','Saved','Applied','Interview','Rejected','Offer'].map(x=><option key={x}>{x}</option>)}</select></label><label><select value={mode} onChange={e=>setMode(e.target.value)}><option>All</option><option>Remote</option><option>Hybrid</option><option>On-site</option></select></label><label>Sort<select value={sort} onChange={e=>setSort(e.target.value)}><option value="match">Highest match, then newest</option><option value="date">Newest</option></select></label></div><section className="panel"><JobTable jobs={displayed} onEdit={job=>setEdit(job)}/></section>{(modal||edit)&&<JobForm job={edit} onClose={()=>{setModal(false);setEdit(null)}}/>}</>}
